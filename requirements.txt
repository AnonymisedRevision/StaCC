# Pools and caches (GPU): torch, torchvision, timm.  Everything after the caches
# is numpy, scipy and scikit-learn, so the method and every table can be re-run
# on a machine with no GPU and no deep-learning stack.
torch>=2.0
torchvision>=0.15
timm>=0.9
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pillow>=9.0
opencv-python>=4.7
matplotlib>=3.7
# data preparation only
wilds>=2.0
pandas>=1.5
pyarrow>=12.0
huggingface_hub>=0.20
